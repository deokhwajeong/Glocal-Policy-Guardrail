# Glocal Policy Guardrail Configuration

# API Server Configuration
# Default: 127.0.0.1 (localhost only - safe for development)
# For production, use specific IP or 0.0.0.0 (all interfaces) with proper firewall
GUARDRAIL_HOST=127.0.0.1

# API Server Port
# Default: 8000
GUARDRAIL_PORT=8000

# Logging Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
LOG_LEVEL=INFO
